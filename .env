# 环境变量配置文件
# 注意：此文件包含敏感信息，不应提交到代码仓库

# 所有认证相关配置（Cookie、Token 等）已迁移到 /data/changdu-web/auth.json
# 开发环境使用 server/data/auth.json

CHANGDU_REVERSE_DIR=/data/changdu-web/reverse-bogus

# 本地开发默认走本地 Koa（http://localhost:3000）
# 如需让本地前端直接访问线上接口，请在 .env.local 中启用：
VITE_PROXY_TARGET=https://cxyy.top
